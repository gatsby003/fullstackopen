{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "backend for phonebook app developed in part 2",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../../part2/phonebook/ && npm run build --prod && cp -r build ../../part3/phonebook-backend/",
    "deploy": "git pull && git subtree push --prefix part3/phonebook-backend heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "start": "node index.js",
    "dev": "nodemon --delay 500ms index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gatsby",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
